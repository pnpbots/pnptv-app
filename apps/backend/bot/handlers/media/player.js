const logger = require('../../../utils/logger');

/**
 * Media player handlers (placeholder).
 * The original player handlers are not present in this deployment.
 */
const registerPlayerHandlers = () => {
  logger.warn('Media player handlers not configured; skipping player routes');
};

module.exports = registerPlayerHandlers;
