const logger = require('../../../utils/logger');

/**
 * Call feedback handlers (placeholder).
 * The original feedback flow is not available in this deployment.
 */
const registerCallFeedbackHandlers = () => {
  logger.warn('Call feedback handlers not configured; skipping call feedback routes');
};

module.exports = registerCallFeedbackHandlers;
