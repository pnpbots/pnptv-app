'use strict';

/**
 * channelPricing.js
 * Stripe was removed 2026-05-28. This file is retained to avoid breaking
 * any require() calls; the map is intentionally empty.
 */

const CHANNEL_PRICE_MAP = {};

module.exports = { CHANNEL_PRICE_MAP };
